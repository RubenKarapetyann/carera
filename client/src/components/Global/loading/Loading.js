import styles from "./Loading.module.css"

const Loading = ()=>{
    return (
        <div className={styles["loading-container"]}>
            Loading...
        </div>
    )
}

export default Loading